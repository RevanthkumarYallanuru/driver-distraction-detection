steps to execute:\
1. after cloning this repo migrate to the source folder driver-distraction-detection

2\. Run this 

```
python -m venv venv
```

3. then 👇

```
venv\Scripts\activate
```

4. Install all dependencies

```
python -m pip install --upgrade pip
pip install -r requirements.txt
```

5. Run the below command to run the application successfully.

```
python src/driver_monitor.py
```